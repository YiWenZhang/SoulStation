

---
# 1.登陆注册
以下是根据你提供的最新代码重写的接口文档。本次更新主要补充了**密码长度**和**手机号格式**的校验规则及对应的报错信息。

---

### 一、 注册接口 (Register)

**功能描述**：用户或管理员注册。包含手机号格式校验、密码长度校验及管理员密钥认证。
**接口地址**：`POST /api/auth/register`
**Content-Type**：`application/json`

#### 1. 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phone` | string | **是** | 手机号 (必须为11位纯数字) |
| `password` | string | **是** | 密码 (长度限制 6-20 位) |
| `role` | string | 否 | 角色，可选值：`"user"` (默认) 或 `"admin"` |
| `admin_key` | string | **条件必填** | 当 `role` 为 `"admin"` 时必填 |
| `nickname` | string | 否 | 昵称，不填则系统自动生成 |

#### 2. 请求示例 (JSON)

**场景 A：注册普通用户**

```json
{
  "phone": "13800138000",
  "password": "my_secret_password"
}

```

**场景 B：注册管理员**

```json
{
  "phone": "13900000000",
  "password": "admin_password",
  "role": "admin",
  "admin_key": "SoulStation2025_Admin" 
}

```

#### 3. 响应示例 (Success)

**HTTP Status: 200 OK**

```json
{
  "code": 200,
  "msg": "用户注册成功，请登录", 
  "data": null
}

```

#### 4. 异常情况与状态码对照表 (Errors)

**注意**：本次更新新增了 `密码长度` 和 `手机号格式` 的校验，错误码均为 **400**。

| HTTP Status | JSON Code | JSON Msg (提示信息) | 原因分析 |
| --- | --- | --- | --- |
| **400** | 400 | "请提供注册信息" | 请求体为空 |
| **400** | 400 | "密码长度不能少于6位" | **【新增】** 密码长度 < 6 |
| **400** | 400 | "密码长度不能超过20位" | **【新增】** 密码长度 > 20 |
| **400** | 400 | "请输入有效的11位手机号码" | **【新增】** 手机号长度非11位或包含非数字字符 |
| **400** | 400 | "手机号和密码不能为空" | 字段缺失 |
| **400** | 400 | "无效的用户角色" | role 字段非法 |
| **400** | 400 | "该手机号已注册，请直接登录" | 数据库中已存在该手机号 |
| **403** | 403 | "管理员密钥错误，无法注册" | 注册管理员时密钥验证失败 |
| **500** | 500 | "注册失败: [错误详情]" | 服务器数据库异常 |

---

### 二、 登录接口 (Login)

**功能描述**：用户登录，验证账号密码，返回 Token 和用户角色。
**接口地址**：`POST /api/auth/login`
**Content-Type**：`application/json`

#### 1. 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phone` | string | 是 | 手机号 |
| `password` | string | 是 | 密码 |

#### 2. 请求示例 (JSON)

```json
{
  "phone": "13800138000",
  "password": "my_secret_password"
}

```

#### 3. 响应示例 (Success)

**HTTP Status: 200 OK**

```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "uid": 1001,
    "nickname": "SoulUser_0000",
    "avatar_url": "...",
    "role": "user",  // 前端根据此字段判断跳转：'user' -> 首页, 'admin' -> 后台
    "token": "eyJhbGciOiJIUzI1..." 
  }
}

```

#### 4. 异常情况与状态码对照表 (Errors)

| HTTP Status | JSON Code | JSON Msg (提示信息) | 原因分析 |
| --- | --- | --- | --- |
| **400** | 400 | "请输入账号密码" | 缺少 phone 或 password 字段 |
| **401** | 401 | "账号或密码错误" | 用户不存在或密码校验失败 |



# 2. 首页 (Home Page) - 测评方式选择

**前端任务说明**：

* **调用时机**：仅在用户 **已登录** 时调用。
* **数据展示逻辑**：
* **顶部导航**：使用 `user_info` 渲染头像和昵称。
* **改善追踪**：根据 `tracking_reminder.show` 判断是否在核心入口附近展示“复测提醒 Banner”。
* **历史记录**：在页面下方展示 `history_records` 列表（如最近的一条或多条）。



#### 接口定义

* **接口地址**：`GET /api/home/index`
* **请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `uid` | Number | **是** | 用户ID |

* **响应数据 (Response)**：

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    // 1. 用户基础信息 (用于顶部导航)
    "user_info": {
      "nickname": "SoulUser_01",
      "avatar_url": "https://example.com/avatar.png"
    },

    // 2. 改善追踪提醒 (PRD: "展示改善追踪提醒")
    "tracking_reminder": {
      "show": true,                  // true=显示提醒, false=不显示
      "message": "距离上次测评已过30天，建议进行复测以追踪改善情况。"
    },

    // 3. 历史记录 (PRD: "展示历史记录")
    // 返回最近的测评记录摘要，用于首页展示
    "history_records": [
      {
        "id": 101,
        "date": "2025-12-01",
        "mode": "ai_chat",           // 测评方式
        "risk_level": "mild",        // 风险等级
        "summary": "轻度焦虑倾向"     // 结果标签
      }
    ]
  }
}

```

---

# 3.题库测评模块接口文档
**Base URL**: `/api`
**Content-Type**: `application/json`

---
## 1. 全局状态码说明 (Response Codes)

所有接口返回的 JSON 中 `code` 字段的含义如下：

| Code | 说明 | 触发场景示例 |
| --- | --- | --- |
| **200** | **成功** | 请求成功，业务逻辑正常执行。 |
| **400** | **请求错误/业务校验失败** | 参数缺失、会话无效、题目未答完即提交。 |
| **401** | **未认证** | 请求中未携带 `uid`，用户未登录。 |
| **403** | **禁止访问** | 权限不足（本模块暂未涉及，但在注册模块有）。 |
| **404** | **资源不存在** | 请求的 `session_id` 在数据库中找不到。 |
| **500** | **服务器内部错误** | 数据库写入失败、代码抛出未捕获异常。 |

---

## 2. 接口详情

### 2.1 初始化/检查测评状态 (Start/Check)

**描述**：用户进入测评页时调用。用于判断是恢复上次未完成的答题进度（断点续传），还是强制开启新的测评。

* **URL**: `/assessment/questionnaire/start`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `uid` | Integer | **是** | 当前登录用户的 ID。 |
| `action` | String | 否 | **操作模式**。<br>

<br>- `check`: (默认) 检查是否有未完成的存档。如果有则恢复，没有则新建。<br>

<br>- `new`: 强制开启新测评（如果有旧的未完成会话，会将其标记为 `abandoned`）。 |

#### 响应参数 (Response Data)

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `session_id` | Integer | **会话ID**。后续保存和提交接口必须传此ID。 |
| `status` | String | 会话状态，固定为 `ongoing` (进行中)。 |
| `is_resumed` | Boolean | **是否为存档**。<br>

<br>- `true`: 恢复了旧进度，前端应弹窗提示用户。<br>

<br>- `false`: 全新开启的测评。 |
| `current_progress_index` | Integer | 上次答题停留的索引（第几题，从0开始）。 |
| `answers_snapshot` | Object | **答案快照**。键值对 `{ "题目ID": 分数 }`，用于前端回显已选选项。 |

#### 响应示例

```json
// 场景：发现存档 (Code 200)
{
    "code": 200,
    "msg": "发现未完成的测评",
    "data": {
        "session_id": 55,
        "status": "ongoing",
        "is_resumed": true,
        "current_progress_index": 12,
        "answers_snapshot": {
            "1": 1,
            "2": 3
        }
    }
}

// 场景：开启新测评 (Code 200)
{
    "code": 200,
    "msg": "开启新测评",
    "data": {
        "session_id": 56,
        "status": "ongoing",
        "is_resumed": false,
        "current_progress_index": 0,
        "answers_snapshot": {}
    }
}

```

---

### 2.2 获取全量题目 (Fetch Questions)

**描述**：一次性获取所有启用的 SCL-90 题目及选项。

* **URL**: `/assessment/questionnaire/questions`
* **Method**: `GET`

#### 请求参数

无

#### 响应参数 (Response Data)

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `questions` | List | 题目列表。 |
| └ `id` | Integer | 题目ID (Question ID)。 |
| └ `stem` | String | 题干内容（如“头痛”）。 |
| └ `type` | String | 题型（固定为 `single_choice`）。 |
| └ `options` | List | 选项列表。 |
|    └ `id` | Integer | 选项ID。 |
|    └ `label` | String | 选项显示的文字（如“没有”、“严重”）。 |
|    └ `score` | Integer | **选项分值** (1-5分)。前端需记录此值用于提交。 |

#### 响应示例

```json
{
    "code": 200,
    "msg": "获取成功",
    "data": {
        "questions": [
            {
                "id": 1,
                "stem": "头痛",
                "type": "single_choice",
                "options": [
                    { "id": 101, "label": "没有", "score": 1 },
                    { "id": 102, "label": "很轻", "score": 2 },
                    // ... 其他选项
                ]
            }
            // ... 共90题
        ]
    }
}

```

---

### 2.3 实时保存进度 (Save Progress)

**描述**：用户答题过程中（如点击下一题、暂停退出时）调用。支持**增量更新**，即不需要每次传所有答案，只需传新修改的答案。

* **URL**: `/assessment/questionnaire/save`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `session_id` | Integer | **是** | 当前测评的会话ID。 |
| `current_index` | Integer | 否 | 当前停留在第几题的索引（用于恢复进度条）。 |
| `answers` | Object | 否 | **答案键值对**。<br>

<br>格式：`{ "题目ID": 分数 }`<br>

<br>示例：`{"5": 3}` 表示第5题选了3分。如果该题已答过，新值会覆盖旧值。 |

#### 响应参数

无特殊 Data 返回。

#### 响应示例

```json
// 成功 (Code 200)
{
    "code": 200,
    "msg": "进度已保存"
}

// 失败：会话无效或已结束 (Code 400)
{
    "code": 400,
    "msg": "会话无效或已结束"
}

```

---

### 2.4 提交并生成报告 (Submit)

**描述**：在最后一题点击“提交”时调用。后端会进行**完整性校验**、SCL-90 因子分计算、风险评级并生成报告。

* **URL**: `/assessment/questionnaire/submit`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `session_id` | Integer | **是** | 当前测评的会话ID。 |

#### 响应参数 (Response Data)

| 参数名 | 类型 | 说明 |
| --- | --- | --- |
| `report_id` | Integer | **报告ID**。前端拿到后应跳转至报告详情页（如 `/report/:id`）。 |
| `total` | Integer | (仅报错时返回) 题目总数。 |
| `answered` | Integer | (仅报错时返回) 已答题目数。 |

#### 响应示例

**场景 A：提交成功 (Code 200)**

```json
{
    "code": 200,
    "msg": "报告生成成功",
    "data": {
        "report_id": 108
    }
}

```

**场景 B：提交失败 - 题目未答完 (Code 400)**

> **注意**：前端应根据此错误码拦截跳转，并提示用户继续作答。

```json
{
    "code": 400,
    "msg": "还有 5 道题目未完成，请继续作答",
    "data": {
        "total": 90,
        "answered": 85
    }
}

```

---

## 3. 业务枚举值字典 (Data Dictionary)

后端逻辑中涉及的内部状态值含义如下：

### 3.1 会话状态 (AssessmentSession.status)

| 值 | 含义 |
| --- | --- |
| `ongoing` | **进行中**。用户正在答题，可以调用 save 接口。 |
| `completed` | **已完成**。用户已提交，报告已生成，不可再修改答案。 |
| `abandoned` | **已废弃**。用户选择了 `action='new'` 重新开始，旧的存档被废弃。 |

### 3.2 风险等级 (AssessmentReport.risk_level)

由后端算分逻辑自动生成：
| 值 | 含义 | 判定规则 (基于SCL-90因子分) |
| :--- | :--- | :--- |
| `good` | **良好** | 所有维度因子分均 < 2.0 |
| `moderate` | **中度风险** | 有 1~2 个维度的因子分 >= 2.0 |
| `severe` | **严重风险** | 有 3 个及以上维度的因子分 >= 2.0 |

### 3.3 选项分值 (Option Score)

| 分值 | 对应 SCL-90 语义 |
| --- | --- |
| 1 | 没有 |
| 2 | 很轻 |
| 3 | 中等 |
| 4 | 偏重 |
| 5 | 严重 |

---
# 4.问卷结果模块
## 接口 8：获取测评报告详情 (Get Report Detail)
该接口用于在测评结束后，或在“历史记录”页面，根据 `report_id` 获取完整的测评报告数据。返回的数据包含**结构化图表数据**（用于前端渲染雷达图）和**Markdown 文本**（用于展示详细建议）。
### 1. 接口基本信息

* **URL**: `/api/assessment/report/detail`
* **Method**: `GET`
* **Auth**: 需要登录 (虽未强制校验 Token，但需要校验 `uid` 与报告归属权)

### 2. 请求参数 (Query Parameters)

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `report_id` | String/Int | 是 | 报告的唯一 ID | `1024` |
| `uid` | String/Int | 是 | 当前登录用户的 ID (用于权限校验) | `10086` |

### 3. 响应示例 (Success 200)

```json
{
    "code": 200,
    "msg": "获取成功",
    "data": {
        "base_info": {
            "report_no": "RPT-1024",
            "date": "2025-12-25",
            "user_name": "测试员小张",
            "mode_name": "SCL-90 专业量表测评"
        },
        "core_result": {
            "risk_level": "moderate",
            "risk_color": "orange",
            "summary_label": "在 焦虑, 抑郁 等方面存在一定心理压力",
            "score_interpretation": "SCL-90采用1-5分评分制，分数越高代表症状越明显。≥2分提示存在轻度症状。"
        },
        "charts": {
            "radar_data": [
                { "name": "焦虑", "value": 4.0, "fullMark": 5 },
                { "name": "抑郁", "value": 2.1, "fullMark": 5 },
                { "name": "躯体化", "value": 1.5, "fullMark": 5 }
            ]
        },
        "content": {
            "advice_md": "### 1. 整体状态总结\n\n您目前的心理状态主要表现为**中度风险**... (此处为长文本 Markdown)"
        },
        "actions": {
            "can_chat": true,
            "can_download": true
        }
    }
}

```

### 4. 字段详细说明

* **`core_result.risk_level`**: 风险等级，前端可用于显示不同颜色的标签或背景。
* `good`: 状态良好 (Green)
* `moderate`: 中度风险 (Orange)
* `severe`: 严重/高风险 (Red)


* **`charts.radar_data`**: 专门适配 ECharts 雷达图的数据格式。
* `value`: 该维度的因子分（均分）。
* `fullMark`: 满分刻度（固定为 5）。


* **`content.advice_md`**: **核心内容**。这是一段完整的 Markdown 文本，前端需要使用 Markdown 渲染组件（如 `v-html` + `marked` 或 `vue-markdown`）进行展示。

### 5. 错误码说明 (Error Codes)

前端需根据 `code` 做相应提示：

| http status | code | msg | 原因与处理建议 |
| --- | --- | --- | --- |
| 400 | 400 | `参数缺失` | 前端未传 `report_id` 或 `uid`。 |
| 404 | 404 | `报告不存在` | 传入的 `report_id` 在数据库里找不到，可能是 ID 传错了。 |
| 403 | 403 | `无权查看此报告` | **安全拦截**。当前登录用户 (`uid`) 试图查看别人的报告。前端应提示“无权访问”并跳转回首页。 |
| 500 | 500 | `(系统报错信息)` | 服务器内部错误。 |

---

### 附录：Markdown 报告格式说明

后端生成的 `advice_md` 字段遵循标准 Markdown 语法，包含以下 4 个标准模块。前端在渲染时，建议为 `h3` (`###`) 标签设置明显的样式。

#### 模块 1：整体状态总结

* **功能**：对用户的心理状态进行定性（良好/中度/高风险），并给出总体评价。
* **示例**：
> ### 1. 整体状态总结
> 
> 
> 您目前的心理状态主要表现为**中度风险**。数据显示您在某些维度上承受了较为明显的困扰...



#### 模块 2：维度详细解读

* **功能**：如果用户有高风险维度（分值≥2），会逐个列出并解释；如果全正常，则显示正面肯定。
* **格式**：`**【维度名】 (评分 x.x)**` + 无序列表说明。
* **示例**：
> ### 2. 维度详细解读
> 
> 
> **【焦虑】 (评分 4.0)**
> * 重度：焦虑情绪频繁（如持续心慌、过度担忧），生理反应明显...
> 
> 



#### 模块 3：通用调节方法

* **功能**：提供通用的心理自助建议（冥想、运动、社交）。
* **格式**：无序列表。
* **示例**：
> ### 3. 通用调节方法
> 
> 
> * **正念冥想**：每天坚持10分钟...
> * **规律运动**：有氧运动能促进多巴胺分泌...
> 
> 



#### 模块 4：专业支持指引

* **功能**：免责声明与就医引导。
* **示例**：
> ### 4. 专业支持指引
> 
> 
> 如果您感到无法独自应对当前的情绪...**请务必寻求专业帮助**...