

---
# 1.登陆注册
以下是根据你提供的最新代码重写的接口文档。本次更新主要补充了**密码长度**和**手机号格式**的校验规则及对应的报错信息。

---

### 一、 注册接口 (Register)

**功能描述**：用户或管理员注册。包含手机号格式校验、密码长度校验及管理员密钥认证。
**接口地址**：`POST /api/auth/register`
**Content-Type**：`application/json`

#### 1. 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phone` | string | **是** | 手机号 (必须为11位纯数字) |
| `password` | string | **是** | 密码 (长度限制 6-20 位) |
| `role` | string | 否 | 角色，可选值：`"user"` (默认) 或 `"admin"` |
| `admin_key` | string | **条件必填** | 当 `role` 为 `"admin"` 时必填 |
| `nickname` | string | 否 | 昵称，不填则系统自动生成 |

#### 2. 请求示例 (JSON)

**场景 A：注册普通用户**

```json
{
  "phone": "13800138000",
  "password": "my_secret_password"
}

```

**场景 B：注册管理员**

```json
{
  "phone": "13900000000",
  "password": "admin_password",
  "role": "admin",
  "admin_key": "SoulStation2025_Admin" 
}

```

#### 3. 响应示例 (Success)

**HTTP Status: 200 OK**

```json
{
  "code": 200,
  "msg": "用户注册成功，请登录", 
  "data": null
}

```

#### 4. 异常情况与状态码对照表 (Errors)

**注意**：本次更新新增了 `密码长度` 和 `手机号格式` 的校验，错误码均为 **400**。

| HTTP Status | JSON Code | JSON Msg (提示信息) | 原因分析 |
| --- | --- | --- | --- |
| **400** | 400 | "请提供注册信息" | 请求体为空 |
| **400** | 400 | "密码长度不能少于6位" | **【新增】** 密码长度 < 6 |
| **400** | 400 | "密码长度不能超过20位" | **【新增】** 密码长度 > 20 |
| **400** | 400 | "请输入有效的11位手机号码" | **【新增】** 手机号长度非11位或包含非数字字符 |
| **400** | 400 | "手机号和密码不能为空" | 字段缺失 |
| **400** | 400 | "无效的用户角色" | role 字段非法 |
| **400** | 400 | "该手机号已注册，请直接登录" | 数据库中已存在该手机号 |
| **403** | 403 | "管理员密钥错误，无法注册" | 注册管理员时密钥验证失败 |
| **500** | 500 | "注册失败: [错误详情]" | 服务器数据库异常 |

---

### 二、 登录接口 (Login)

**功能描述**：用户登录，验证账号密码，返回 Token 和用户角色。
**接口地址**：`POST /api/auth/login`
**Content-Type**：`application/json`

#### 1. 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `phone` | string | 是 | 手机号 |
| `password` | string | 是 | 密码 |

#### 2. 请求示例 (JSON)

```json
{
  "phone": "13800138000",
  "password": "my_secret_password"
}

```

#### 3. 响应示例 (Success)

**HTTP Status: 200 OK**

```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "uid": 1001,
    "nickname": "SoulUser_0000",
    "avatar_url": "...",
    "role": "user",  // 前端根据此字段判断跳转：'user' -> 首页, 'admin' -> 后台
    "token": "eyJhbGciOiJIUzI1..." 
  }
}

```

#### 4. 异常情况与状态码对照表 (Errors)

| HTTP Status | JSON Code | JSON Msg (提示信息) | 原因分析 |
| --- | --- | --- | --- |
| **400** | 400 | "请输入账号密码" | 缺少 phone 或 password 字段 |
| **401** | 401 | "账号或密码错误" | 用户不存在或密码校验失败 |



# 2. 首页 (Home Page) - 测评方式选择

**前端任务说明**：

* **调用时机**：仅在用户 **已登录** 时调用。
* **数据展示逻辑**：
* **顶部导航**：使用 `user_info` 渲染头像和昵称。
* **改善追踪**：根据 `tracking_reminder.show` 判断是否在核心入口附近展示“复测提醒 Banner”。
* **历史记录**：在页面下方展示 `history_records` 列表（如最近的一条或多条）。



#### 接口定义

* **接口地址**：`GET /api/home/index`
* **请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `uid` | Number | **是** | 用户ID |

* **响应数据 (Response)**：

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    // 1. 用户基础信息 (用于顶部导航)
    "user_info": {
      "nickname": "SoulUser_01",
      "avatar_url": "https://example.com/avatar.png"
    },

    // 2. 改善追踪提醒 (PRD: "展示改善追踪提醒")
    "tracking_reminder": {
      "show": true,                  // true=显示提醒, false=不显示
      "message": "距离上次测评已过30天，建议进行复测以追踪改善情况。"
    },

    // 3. 历史记录 (PRD: "展示历史记录")
    // 返回最近的测评记录摘要，用于首页展示
    "history_records": [
      {
        "id": 101,
        "date": "2025-12-01",
        "mode": "ai_chat",           // 测评方式
        "risk_level": "mild",        // 风险等级
        "summary": "轻度焦虑倾向"     // 结果标签
      }
    ]
  }
}

```

---

# 3.题库测评模块接口文档

**Base URL**: `/api`
**Content-Type**: `application/json`

---

### 3.1. 初始化/检查测评状态 (Start/Check)

**描述**：用户进入测评页时调用。用于判断是恢复上次未完成的答题进度，还是开启新的测评。

* **URL**: `/assessment/questionnaire/start`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `uid` | Number | 是 | 用户ID |
| `action` | String | 否 | 操作类型。<br>

<br>`check`: (默认) 检查是否有存档，有则恢复，无则新建。<br>

<br>`new`: 强制开启新测评 (会放弃旧存档)。 |

#### 响应示例 (Response)

**场景 A：恢复旧存档 (`is_resumed: true`)**

```json
{
    "code": 200,
    "msg": "发现未完成的测评",
    "data": {
        "session_id": 101,            // 会话ID，后续保存/提交都需要传此ID
        "status": "ongoing",
        "is_resumed": true,           // 标识：前端应弹出"是否继续"的提示
        "current_progress_index": 15, // 上次停留的题目索引 (第16题)
        "answers_snapshot": {         // 已保存的答案，用于回显选中状态
            "1": 1,
            "2": 3,
            "5": 2
        }
    }
}

```

**场景 B：开启新测评 (`is_resumed: false`)**

```json
{
    "code": 200,
    "msg": "开启新测评",
    "data": {
        "session_id": 102,
        "status": "ongoing",
        "is_resumed": false,
        "current_progress_index": 0,
        "answers_snapshot": {}
    }
}

```

---

### 3.2. 获取全量题目 (Fetch Questions)

**描述**：一次性获取 SCL-90 所有题目及选项。前端需在本地缓存题目列表，实现无延迟翻页。

* **URL**: `/assessment/questionnaire/questions`
* **Method**: `GET`

#### 请求参数

无

#### 响应示例 (Response)

```json
{
    "code": 200,
    "msg": "获取成功",
    "data": {
        "questions": [
            {
                "id": 1,
                "stem": "从无缘无故地感到害怕",       // 题干
                "type": "single_choice",            // 题型
                "options": [                        // 选项列表 (已按分数排序)
                    { "id": 101, "label": "没有", "score": 1 },
                    { "id": 102, "label": "很轻", "score": 2 },
                    { "id": 103, "label": "中等", "score": 3 },
                    { "id": 104, "label": "偏重", "score": 4 },
                    { "id": 105, "label": "严重", "score": 5 }
                ]
            },
            {
                "id": 2,
                "stem": "容易哭泣",
                "type": "single_choice",
                "options": [...] 
            }
            // ... 共90条
        ]
    }
}

```

---

### 3.3. 实时保存进度 (Save Progress)

**描述**：用户点击“上一题”、“下一题”或“保存退出”时调用。支持增量更新或修改答案。

* **URL**: `/assessment/questionnaire/save`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `session_id` | Number | 是 | 必须传 `start` 接口返回的 session_id |
| `current_index` | Number | 否 | 当前页面停留的题目索引 (用于下次恢复位置) |
| `answers` | Object | 否 | 键值对：`{"题目ID": 分数}`。<br>

<br>示例：`{"1": 2}` 表示修改第1题答案为2分。 |

#### 响应示例 (Response)

```json
{
    "code": 200,
    "msg": "进度已保存"
}

```

---

### 3.4. 提交测评 (Submit)

**描述**：在最后一题点击“提交”时调用。后端将校验完整性、计算SCL-90因子分、生成报告。

* **URL**: `/assessment/questionnaire/submit`
* **Method**: `POST`

#### 请求参数 (Request Body)

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `session_id` | Number | 是 | 当前会话ID |

#### 响应示例 (Response)

**提交成功**

```json
{
    "code": 200,
    "msg": "报告生成成功",
    "data": {
        "report_id": 55     // 报告ID，前端拿到后跳转至 /report/55
    }
}

```

**提交失败 (未答完)**

```json
{
    "code": 400,
    "msg": "还有题目未完成，无法提交" // 此时前端不应跳转
}

```

---

### 前端开发特别注意 (Notes for Frontend)

1. **答案数据结构**：
* 后端存储的答案结构为 `Map<String, Int>` (即 `{"question_id": score}`)。
* 前端提交时，`answers` 字段可以直接传当前题目的答案（如 `{"5": 3}`），后端会自动做合并更新（Merge），因此不需要每次都传全量答案。


2. **分数处理**：
* SCL-90 标准分值为 1-5 分。虽然界面显示的是“没有/很轻...”，但传给后端的必须是 `options` 里的 `score` 字段值。


3. **断点续传流程**：
* 用户进入页面 -> 调接口1 (`start`)。
* 如果 `is_resumed` 为 `true` -> 弹窗询问“是否继续”。
* 是 -> 设置当前题号为 `current_progress_index`，并根据 `answers_snapshot` 填充已选状态。
* 否 -> 调接口1，传 `action: 'new'` -> 清空状态从头开始。

