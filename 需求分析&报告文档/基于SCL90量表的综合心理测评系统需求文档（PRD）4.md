# 基于SCL90量表的综合心理测评系统需求文档（PRD）

# 一、背景和目标

## 1.1 背景介绍

传统心理测评存在答题冗长、量表适配性弱、结果解读笼统、缺乏专业落地指导等问题，难以满足当代用户高效获取精准心理状态评估与改善建议的需求。随着社会压力加剧，用户对自身心理状态筛查、情绪调节、心理困扰应对等个性化需求凸显，但现有工具多局限于单一量表套用，缺乏结合专业理论与实际场景的针对性分析。本系统以AI Agent为核心，以SCL90（症状自评量表90项）为基础框架，采用「问询式对话+定向题库」双模式，通过轻量化交互精准捕捉用户心理特征，结合SCL90专业评分标准与心理服务数据库，生成科学实用的测评结果及改善方案，破解传统心理测评效率低、匹配弱、落地难的痛点。

注：SCL90量表核心覆盖9个心理症状维度（躯体化、强迫症状、人际关系敏感、抑郁、焦虑、敌对、恐怖、偏执、精神病性），并包含1个附加维度（饮食睡眠），本系统以此为核心测评框架，确保测评的专业性与科学性。

## 1.2 价值分析

### 1.2.1 用户价值

- 高效完成测评：基于SCL90量表核心维度优化题目设计，5-10分钟即可完成全流程交互，避免传统量表冗长答题负担；

- 精准心理评估：获得基于SCL90专业标准的心理状态评分、症状维度分析、风险等级判定等专业解读；

- 个性化落地建议：针对高风险维度（如焦虑、抑郁、人际关系敏感等），提供可直接应用的情绪调节方法、行为改善方案及专业支持指引；

- 轻量化交互体验：支持AI对话自然问询（无需刻意刷题，通过日常化对话捕捉心理特征）或定向题库快速作答，适配不同用户使用场景（如碎片时间/深度自我评估）。

### 1.2.2 业务价值

- 沉淀高价值用户数据：积累用户心理症状标签、风险等级、改善需求等精准数据，构建专业心理用户画像库；

- 支撑精准业务引流：为心理咨询（如情绪疏导、心理困扰干预）、心理培训、心理健康产品等关联业务提供高意向用户引流入口；

- 提升用户留存与活跃度：通过定期更新测评主题（如季节性情绪评估、压力应对能力复测）、个性化结果推送及改善进度追踪，增强用户复访与使用粘性。

## 1.3 核心体验流程

用户进入系统→选择测评方式（AI对话/题库）→完成SCL90核心维度测评（满足各维度最低覆盖要求）→系统执行SCL-90评分逻辑（原始分计算→标准分转换→风险等级判定）→获取专业测评结果→查看/分享历史记录/接收改善追踪提醒

## 1.4 路径图（项目排期）

- 阶段1：需求评审（2025-12）→原型设计（2025-12）

- 阶段2：开发实现（2025.12）→测试验收（2025-12-28）

# 二、核心评分逻辑（新增）

## 2.1 评分基础规范

本系统SCL-90评分严格参照《心理卫生评定量表手册（增订版）》（中国心理卫生杂志社，1999），所有计算规则、判定标准均符合临床通用指南，确保评分结果科学可追溯。

## 2.2 具体评分规则

### 2.2.1 原始分计算

- 维度原始分：某一维度所有作答问题的评分（1-5分）相加；未达最低覆盖要求的维度（核心维度<5题、附加维度<3题），标记为“数据不完整”，不计算原始分，不参与风险判定。

- 总原始分：所有核心维度（数据完整）原始分相加，附加维度原始分不纳入总分计算。

- 缺失项目处理：单个维度内缺失题目≤2题时，按该维度已作答题目平均分补全；缺失题目>2题时，该维度标记为“数据不完整”；全量表缺失题目>10题时，测评无效，提示用户重新测评。

### 2.2.2 标准分转换

- 维度标准分=维度原始分/该维度作答题目数×10（保留1位小数）；标准分范围：10-50分。

- 总标准分=总原始分/所有核心维度作答题目总数×10（保留1位小数）；标准分范围：10-50分。









### 2.2.3 风险等级分界线

|风险等级|维度标准分范围|总标准分范围|对应标签示例|
|---|---|---|---|
|良好（绿色）|10.0-20.0分|10.0-18.0分|心理状态良好、无明显焦虑倾向|
|轻度风险（黄色）|20.1-30.0分|18.1-25.0分|轻度焦虑倾向、轻度人际关系敏感|
|中度风险（橙色）|30.1-40.0分|25.1-32.0分|中度抑郁倾向、中度强迫症状|
|高度风险（红色）|40.1-50.0分|32.1-50.0分|高度焦虑倾向、高度敌对情绪|
### 2.2.4 标签映射规则

- 单维度标签：取风险等级最高的维度，结合维度名称生成（如“焦虑维度35.2分（中度风险）”→“中度焦虑倾向”）。

- 综合标签：若存在多个维度风险等级一致且为最高，取维度名称组合生成（如“焦虑、抑郁维度均为中度风险”→“中度焦虑伴抑郁倾向”）；若总标准分风险等级高于各维度最高等级，以总标准分对应标签为主（如“总标准分26.5分（中度风险），各维度最高为轻度”→“中度心理困扰倾向”）。

# 三、页面设计

## 3.1 用户端页面

### 3.1.1 登录/注册页（独立页面）

#### 功能需求

##### P0

- 页面类型区分：

    - 页面顶部提供“普通用户”“管理员”切换选项，默认显示普通用户登录界面。

    - 切换后同步更新登录/注册表单及相关提示文案，清晰区分两类用户入口。

- 普通用户端登录/注册：

    - 登录功能：默认显示手机号/邮箱 + 密码登录表单；提供“验证码登录”选项，点击后切换为输入手机号/邮箱获取验证码的登录形式。

    - 注册引导：登录表单下方明显标注“新用户？立即注册”，点击后切换至普通用户注册表单（需填写手机号/邮箱、获取验证码、设置密码）。

- 管理员端登录/注册：

    - 登录功能：显示管理员账号（手机号/邮箱） + 密码登录表单，无验证码登录选项。

    - 注册引导：登录表单下方标注“管理员注册”，点击后切换至管理员注册表单，除填写手机号/邮箱、获取验证码、设置密码外，需额外填写“管理员密钥”进行认证，密钥验证通过后方可完成注册。

- 跳转首页逻辑：

    - 普通用户/管理员登录/注册成功后，自动跳转至首页（测评方式选择页面）。

##### P1

- 登录/注册辅助功能：

    - 普通用户/管理员登录界面均提供“忘记密码”选项，点击后跳转至密码找回页面（通过手机号/邮箱验证码找回）。

- 页面底部显示隐私政策、用户协议链接（普通用户对应普通用户协议，管理员对应管理员协议）。

- 首页跳转入口：

    - 页面角落提供“返回首页”链接，点击后跳转至测评方式选择首页（未登录状态下首页仅展示系统价值信息及“登录/注册”入口按钮）。

- 系统价值展示：

    - 页面侧边/底部展示系统核心价值信息（如“SCL90专业测评，5分钟读懂你的心理状态”），增强用户认知。

#### 质量需求

##### P0

- 界面引导性：视觉突出“普通用户/管理员”切换选项及核心登录/注册按钮，引导明确。

- 安全性：管理员密钥认证需实时校验有效性，校验失败给出明确提示；所有输入字段需进行格式校验（如手机号合法性、密码复杂度）。

### 3.1.2 首页（测评方式选择）

#### 功能需求

##### P0

- 核心入口展示：

    - 页面视觉中心采用并列的大卡片式设计，展示“AI对话测评”和“题库答题测评”入口。

    - 每个卡片包含：

        - 具有辨识度的图标（对话框图标代表AI，问卷图标代表题库）

        - 明确标题（“AI对话测评”“题库答题测评”）、简短描述（AI对话测评：“像聊天一样完成评估，轻松无压力”；题库答题测评：“SCL90核心题集，快速精准出结果”）。

- 页面内容区分：

    - 已登录状态：除核心测评入口外，展示历史记录、改善追踪提醒等内容。

    - 未登录状态：仅展示核心测评入口及系统价值信息，点击测评入口后跳转至登录/注册页。

- 导航功能：

    - 已登录状态下，顶部显示用户头像/昵称（点击可进入个人中心）和“我的测评”入口。

- 

##### P1

- 加载状态：AI“正在思考...”时，显示加载动画（如气泡内的“...”动画），同时可显示“正在梳理您的回答，请稍候”提示文字。

- 对话记录回溯：支持用户向上滚动查看历史对话内容，便于用户回顾自身回答逻辑。

- 情绪安抚提示：当用户表述负面情绪时，AI可附加温和安抚提示（如“您的感受我们能理解，这是正常的情绪表达”）。

- 页面底部包含版权信息、隐私政策、用户协议等链接。

- 界面引导性：视觉突出核心测评入口，通过色彩对比、图标大小等设计强化引导明确性。

- 流程最短路径：已登录用户点击测评入口后，最多一步确认（如选择“全面测评”/“重点维度测评”）后即开始测评，流程极简。

- 隐私安全感：在页面显著位置标注隐私保护承诺，降低用户心理数据泄露顾虑。

#### 质量需求

- 对话连贯性：AI的提问和追问必须符合上下文逻辑，贴合SCL90测评维度递进要求，保持对话的连贯性和深度，避免跳跃式问询；同一维度的问题需集中问询，完成后再切换至下一维度。

- 语言温和性：AI所有表述需采用温和、中立、支持性的语言风格，避免刺激性、评判性表述。

- 维度完整性保障：测评结束后，系统自动校验各维度覆盖情况，未达最低覆盖要求的维度需在结果中明确标注，且该维度不参与风险等级判定。



### 3.1.3 题库答题测评页、

#### 功能需求

##### P0

- 题目展示规范：

    - 单题单屏展示：当前题目、选项独占一屏，避免滚动干扰；页面四周留有充足留白，提升阅读舒适度。

    - 题型清晰标注：在题目旁明确标注“单选”（SCL90核心题型为单选，基于“没有、很轻、中等、偏重、严重”5级评分）。

    - 题目来源标注：每题下方可标注“基于SCL90专业量表”，增强专业性可信度。

- 选项设计：

    - 选项按“SCL90 5级评分标准”设计，统一表述为“没有、很轻、中等、偏重、严重”，并对应清晰的分值（后台记录，用户端不显示）。

    - 选项采用横向排列或纵向排列（根据屏幕尺寸自适应），每个选项为独立可点击区域，选中后有明显视觉标识（如底色变色、勾选标记）。

- 导航按钮与状态:

    - 页面底部固定显示“上一题”、“下一题”、“保存退出”按钮，按钮尺寸适中，便于点击。

    - “下一题”按钮在未选择答案时置灰禁用，选择答案后自动激活。

    - 第一题时，“上一题”按钮置灰禁用；最后一题时，“下一题”按钮替换为“提交测评”按钮。

- 答案修改：支持通过“上一题”按钮回退到已答题目并修改答案，修改后答案实时保存，返回当前题目时显示修改后的选中状态。

##### P1

- 进度指示器：在页面顶部固定显示当前进度（如“3/45”，SCL90核心题量45题，可根据优化需求调整），进度条同步展示完成比例。

- 进度保存与提示

    - 点击“保存退出”后，系统自动保存当前答题进度，弹出toast提示“进度已保存，下次可继续作答”。

    - 再次进入题库测评时，自动提示“检测到您有未完成的测评，是否继续？”，提供“继续作答”和“重新开始”选项。

- 答题时长提示：顶部进度条旁可显示预估剩余时间（如“预估剩余5分钟”），帮助用户合理安排时间。

#### 质量需求

- 题目可读性：题目文字清晰，字体大小适中（建议正文16px），行间距合理（1.5倍），避免阅读疲劳。

- 操作稳定性：答案选择、修改、进度保存等操作响应及时，无卡顿、数据丢失现象；网络中断后重新连接，可恢复当前答题进度。

- 隐私保护性：答题过程中所有数据加密传输，本地缓存数据自动加密，防止心理测评数据泄露。

### 3.1.4 结果详情页 (核心)

#### 功能需求

##### P0

- 报告结构：
        

    - 顶部：显示显眼的“综合心理状态标签”（如“轻度焦虑倾向”、“心理状态良好”、“人际关系敏感偏重”等），标签颜色根据风险等级区分（绿色：良好；黄色：轻度风险；橙色：中度风险；红色：高度风险），点击标签可查看详细解释（含SCL90评分标准说明：原始分、标准分换算过程及对应风险等级依据）。

    - 中部：核心维度雷达图，展示SCL90 9大核心维度的标准分分布（雷达图各维度标注清晰，评分区间可视化）；雷达图下方可添加各维度简要解读（如“焦虑维度评分较高，可能表现为频繁紧张、担忧”）及数据完整性说明（如“所有维度均满足最低覆盖要求，结果可信度高”）。

    - 底部：核心建议文本，按“整体状态总结→高风险维度针对性建议→通用调节方法→专业支持指引”结构撰写；建议文本区域提供“一键复制”功能，点击后提示“建议已复制，可分享给专业心理顾问参考”。

- 基础信息展示：页面顶部显示测评时间、测评方式（AI对话/题库）、报告编号（用于后续查询或专业咨询）。

- 专业支持入口：在报告底部显著位置设置“咨询专业心理顾问”按钮，点击后跳转至心理服务对接页面（如心理咨询师列表、预约表单）。

##### P1

- 分享功能：提供“生成分享图”按钮，点击后3秒内生成包含用户昵称、测评时间、心理状态标签、核心雷达图、关键建议的长图，并提示“生成成功，可分享至微信/保存本地”；分享图需添加隐私保护提示（如“本报告为个人隐私信息，建议谨慎分享”）。

- 改善追踪入口：提供“开启改善追踪”选项，点击后可设置定期复测提醒（如“1个月后复测”、“自定义时间”），系统将在设定时间通过站内信/短信提醒用户复测。

- 报告下载：支持将完整测评报告下载为PDF格式，下载文件命名规范为“用户昵称_心理测评报告_日期.pdf”。

#### 质量需求

- 分享图生成质量：生成的图片需清晰、美观，布局合理，色彩协调；在不同尺寸屏幕上显示正常，无文字/图表变形现象。

内容准确性：报告内容、心理状态标签、维度评分、建议等必须与用户测评结果严格对应，符合SCL90yo专业评分标准，逻辑自洽；禁止出现模糊表述或误导性结论；评分过程需可追溯（后台保留原始作答与评分匹配记录）。

- 语言通俗性：报告解读与建议文本需采用通俗易懂的语言，避免过多专业术语；若必须使用，需附加通俗解释。

- 风险提示规范性：对于中度及以上风险等级的测评结果，需明确提示“建议及时寻求专业心理支持”，并提供正规心理服务机构/热线信息，避免引起用户恐慌或忽视风险。

### 3.1.5 我的测评列表页

#### 功能需求

##### P0

- 列表排序与展示：

    - 记录按测评完成时间倒序排列，最新记录置顶。

    - 每条记录卡片包含：测评方式图标（AI对话/题库）、完成日期、综合心理状态标签（带风险等级颜色标识）、“查看报告”按钮。

    - 交互操作：

        - 支持下拉刷新以更新列表，获取最新测评记录。

        - 点击“查看报告”按钮，直接跳转至对应测评的结果详情页。

- 时间维度横向结果对比：
        

    - 核心功能：自动汇总用户历史测评记录，按时间顺序（如近3个月/半年/1年）生成各核心维度评分变化趋势图（柱状图/折线图），直观展示心理状态变化情况。

    - 辅助细节：支持自定义时间范围筛选（如近1个月/3个月/自定义区间）、指标维度切换（单指标/多指标对比）；图表支持下载/分享，下载格式为PNG/PDF。

- 心理顾问AI Agent分析入口：
        

    - 核心功能：在列表顶部或每条记录旁增设「AI顾问解读」入口，点击后可直接发起对话，AI Agent基于当前及历史测评数据，提供专业心理状态变化分析、持续改善建议及个性化干预方案。

    - 对话支持：AI可解答用户关于测评结果的疑问（如“我的焦虑维度评分下降代表什么”），并根据历史数据推荐适合的调节方法。

##### P1

- 空状态处理：无历史测评记录时，显示友好的空状态提示图和文案（如“尚未完成任何测评，点击下方按钮开始首次心理评估吧”），并提供“立即测评”入口按钮。

- 测评提醒设置：支持用户设置定期测评提醒（如每月1次），系统通过站内信/短信发送提醒通知。

- 记录管理：支持删除过期测评记录（删除前需二次确认），仅保留用户希望留存的记录。

#### 质量需求

- 数据实时性：下拉刷新后，列表数据必须是最新的，包括刚完成的测评记录；趋势图数据与测评报告数据严格一致，无偏差。

- 图表可读性：趋势图坐标轴标注清晰，数据点/柱状图识别明确，支持鼠标悬浮查看具体评分及时间；图表配色协调，避免视觉混乱。

- 隐私安全性：删除测评记录时，需彻底清除前端展示及后端数据库中的对应数据（含历史趋势图关联数据），无法恢复；避免用户心理数据残留泄露。

### 3.1.6 用户主页（个人中心）

#### 功能需求

##### P0

- 基础信息展示：

    - 页面顶部展示用户头像（默认显示占位头像，支持自定义上传）、用户名（系统唯一标识，不可修改）、昵称（可修改）。

    - 展示核心信息卡片：累计测评次数、最近一次测评时间及结果摘要（如“轻度焦虑倾向，2025-12-20”）、账号注册时间。

信息修改功能：

- 昵称修改：提供“编辑”按钮，点击后弹出修改弹窗，输入新昵称（限制2-10个字符，支持中英文、数字、下划线），提交后实时更新并提示“昵称修改成功”。

- 头像修改：点击头像区域弹出“更换头像”选项（拍照/从相册选择），支持预览裁剪（比例建议1:1），上传完成后实时替换原有头像，保留历史头像记录（最多3张）。

- 密码修改：单独设置“密码修改”入口，点击后跳转至密码修改表单，需验证原密码（或通过手机/邮箱验证码验证），再输入新密码（8-20位，含字母、数字、特殊字符至少两种）及确认密码，提交后提示“密码修改成功”，并建议重新登录生效。

安全设置入口：

- 显示绑定信息：展示当前绑定的手机号/邮箱（脱敏显示，如138****1234），提供“更换绑定”按钮，点击后通过原绑定信息验证（验证码）后可修改绑定。

- 账号安全提示：显示“登录设备管理”入口，点击可查看最近3次登录记录（登录时间、设备型号、IP地址），支持对异常设备进行“强制下线”操作。

核心功能入口聚合：

- 清晰展示“我的测评”“开始新测评”“消息通知”等高频功能入口，与对应页面快速跳转。

##### P1

- 个性化设置：支持设置页面主题（浅色/深色模式）、消息通知开关（如测评提醒、系统通知是否开启）。

- 测评偏好设置：可预设常用测评方式（AI对话/题库答题），下次进入首页时自动高亮推荐该入口。

- 帮助与反馈：提供“问题反馈”“使用指南”入口，点击“问题反馈”可直接跳转至反馈表单（支持文字描述+图片上传）；点击“使用指南”可查看测评流程、结果解读等说明文档。

#### 质量需求

- 操作安全性：

    - 密码修改、绑定信息更换等敏感操作必须经过严格验证（原密码/验证码），防止账号被盗用篡改。

    - 头像、昵称等信息上传/修改后需进行内容审核（过滤违规内容），审核通过前显示原信息，审核不通过时提示违规原因并拒绝生效。

交互流畅性：

- 所有修改操作流程极简，最多3步即可完成（如昵称修改：点击编辑→输入新昵称→提交），无多余跳转。

- 信息修改后实时更新展示，无需刷新页面；操作失败时给出明确错误提示（如“原密码错误”“昵称格式不合法”）。

兼容性：头像上传后需适配不同尺寸屏幕展示，裁剪后图像清晰无变形；页面布局在手机、平板等设备上自适应显示。

## 3.2 后台端页面

### 3.2.1 数据管理页

#### 功能需求

##### P0

- 搜索功能：

    - 顶部提供搜索框，支持按用户ID、手机号、测评报告编号进行精确查询。

    - 支持高级搜索：可按测评时间范围、测评方式、心理风险等级（良好/轻度/中度/高度）、核心高风险维度等条件组合筛选。

- 数据表格：
       

    - 支持按“测评日期”、“风险等级”等字段排序（升序/降序）。

    - 表格列内容：用户ID、脱敏手机号、测评时间、测评方式、综合心理状态标签、风险等级、核心高风险维度、操作（查看详情/导出报告）。

    - 分页设置：每页默认显示20条数据，支持手动调整每页显示数量（10/20/50/100条）。

    - 手机号脱敏显示：采用“138****1234”格式，保护用户隐私。

- 批量导出：
        

    - 提供“批量导出”按钮，支持导出筛选后的数据列表或全部数据。

    - 导出过程：点击导出后显示进度条，实时展示导出进度（如“导出中，已完成30%”）；完成后自动下载Excel文件。

    - 导出文件内容：包含表格中所有展示字段，附加用户测评各维度详细评分数据（基于SCL90标准）。

##### P1

- 文件命名：导出文件自动命名，格式为“心理测评数据_筛选条件_年月日.xlsx”（如“心理测评数据_轻度风险_20251220.xlsx”）。

- 数据统计概览：页面顶部显示数据统计卡片，包含“总测评次数”、“今日测评次数”、“各风险等级占比”、“核心维度高风险人数TOP3”等核心指标，支持点击指标查看详情。

- 数据导出记录：记录所有批量导出操作，包含导出人、导出时间、导出范围、文件大小等信息，支持查看导出历史。

#### 质量需求

- 操作反馈：导出进度条需真实反映处理进度，给管理员明确预期；导出成功/失败均需显示清晰提示（如“导出成功，共导出120条数据”、“导出失败，请重试”）。

- 数据准确性：表格数据、导出数据与用户实际测评结果严格一致，无遗漏、错漏字段；统计概览指标计算准确，实时同步最新数据。

- 隐私保护性：所有导出数据中用户身份信息（手机号、ID）均需脱敏；导出操作需记录日志，支持追溯，防止违规导出用户心理数据。

- 系统稳定性：批量导出大量数据（如1000条以上）时，系统无卡顿、崩溃现象；支持后台异步导出，管理员可继续其他操作，导出完成后接收通知。

### 3.2.2 题库管理页

#### 功能需求

##### P0

- 分类管理

    - 左侧显示题目分类树，按SCL90核心维度分类（躯体化、强迫症状、人际关系敏感、抑郁、焦虑、敌对、恐怖、偏执、精神病性、附加维度（饮食睡眠））。

    - 支持新增子分类（如焦虑维度下新增“社交焦虑”、“广泛性焦虑”子分类）、编辑分类名称、删除分类（删除前需确认该分类下无题目或先转移题目）。

- 题目管理：

    - 右侧显示当前分类下的题目列表，包含题目ID、题干、题型、关联维度、状态（启用/禁用）、操作（编辑/删除/预览）。

    - 支持批量勾选题目并删除（删除前二次确认）；支持按题目ID、题干关键词搜索题目。

- 题目配置：
        

    - 新增/编辑题目时，需配置以下信息

        - 题干：需符合SCL90量表专业规范，语言准确无歧义。

        - 选项：固定为“SCL90 5级评分选项”（没有、很轻、中等、偏重、严重），无需自定义；需配置各选项对应的分值（如1-5分，后台记录）。

        - 关联维度：必选项，需选择对应的SCL90核心维度（可多选，如某题目同时关联“焦虑”和“人际关系敏感”维度）。

        - 题目难度：可选填（简单/中等/复杂），用于后续题库优化。

##### P1

- 状态管理：题目有“启用/禁用”状态，禁用题目不展示给用户；支持批量启用/禁用题目。

- 操作反馈：保存题目、删除题目、修改状态后，弹出清晰提示（如“题目配置成功”、“题目已禁用”）。

- 批量导入/导出

    - 支持批量导入题目提供标准导入模板（含题干、关联维度、分值等字段），上传Excel文件后校验数据格式，显示导入成功/失败数量及失败原因。

    - 支持批量导出当前分类下的题目：导出为Excel文件，包含题目所有配置信息。

#### 质量需求

- 操作安全性：关键操作（删除分类、批量删除题目、批量禁用题目）需二次确认，防止误操作；所有操作记录日志（操作人、操作时间、操作内容、结果），支持追溯。

- 界面效率：支持批量操作、快速搜索、分类筛选，减少管理员重复操作；新增/编辑题目表单逻辑清晰，必填项标注明确，减少配置失误。

- 专业性保障：题目配置需符合SCL90量表专业标准，可在页面添加“SCL90量表规范指引”链接，供管理员参考；避免出现不符合专业标准的题目配置。

### 3.2.3 AI Agent配置页

#### 功能需求

##### P0

- 问题库管理：

    - 问题按SCL90核心维度分类（躯体化、强迫症状、人际关系敏感等），左侧显示分类树，可点击分类展开查看该维度下的问题列表；每个维度问题库需包含至少10个备选问题（满足不同用户场景的问询需求）。

    - 支持新增、编辑、删除问题；新增问题需填写问题内容、选择关联的心理维度（至少一个）、设置问题优先级（1-5级，1级最高）及对应的SCL-90评分匹配规则（明确该问题不同作答内容对应的评分等级）。

    - 问题列表显示问题内容、关联维度、优先级、启用状态、评分匹配规则摘要，支持按维度、优先级筛选。

- 问询逻辑配置：

    - 支持设置AI问询的递进逻辑，如“先询问通用心理状态问题，再根据用户回答聚焦对应维度的细分问题”；同一维度内按问题优先级从高到低问询。

    - 支持配置追问规则：明确模糊作答的判定标准（如“未提及频率/程度的表述”）及对应的追问话术模板，确保追问逻辑统一。

    - 支持配置维度覆盖规则：设置各维度最低覆盖问题数量，未达标时的自动补充问询规则（如优先选择同维度未使用的高优先级问题）。

- 问题预览：

    - 预览过程中支持记录问题（如“AI追问话术不够温和”“评分匹配偏差”），预览结束后可直接跳转至对应问题的编辑页面进行修改。

- 规则配置：

    - 新增问题需关联至少一个SCL90心理维度，且明确评分匹配规则，否则无法保存。

    - 判定规则支持设置优先级，高优先级规则先执行（如高优先级问题优先问询，高优先级判定条件优先触发）。

    - 支持配置AI语言风格规则（如“全程采用温和、支持性语言，避免评判性表述”“禁用专业术语，采用通俗表达”）及禁止表述清单，配置后实时生效并同步至对话逻辑。

    - 支持配置SCL-90评分核心规则（原始分计算、标准分转换公式、风险等级分界线），配置后需经临床顾问审核确认方可生效。

##### P1

- 情绪识别规则配置：支持配置AI对用户负面情绪（如抑郁、焦虑、敌对）的识别关键词及响应策略（如识别到“想自杀”等高危表述时，自动推送心理危机干预热线）。

- 对话日志查看：支持查看用户与AI Agent的真实对话日志，按时间、用户ID筛选，用于优化AI问询逻辑和问题库。

- 版本管理：支持保存AI配置的不同版本（如“202512初始版本”、“202601优化版本”），可回滚至历史版本，避免配置失误无法恢复。

#### 质量需求

- 逻辑连贯性：配置后的AI问询逻辑需符合SCL90测评维度递进要求，确保对话流畅、无跳跃，能全面覆盖核心测评要点；评分匹配规则需与SCL-90标准严格一致，无偏差。

- 语言规范性：AI的问题表述、追问话术需符合配置的语言风格规则，温和、专业、无歧义，避免刺激性表述；禁止表述清单内的内容需严格过滤，无遗漏。

- 配置稳定性：修改配置并保存后，AI的行为需实时同步更新；预览功能能真实反映配置后的效果，无偏差；配置变更需记录版本，支持回滚。

- 高危风险防控：情绪识别规则需精准，对用户高危情绪表述的响应需及时、准确，推送的干预资源需正规、有效，避免遗漏或误判高危风险。

- 评分一致性：同一用户相同作答内容，不同时间测评的评分结果需一致；不同用户相似作答内容，评分结果需符合统一标准，误差范围≤0.5分（标准分）。

- 



### 3.2.5 辅助页面（可选）

#### 2.2.5.1 数据导出结果页

- 导出范围设定：支持按时间范围（如近7天/近30天/自定义日期）、用户群体（如某一部门用户/全量用户）、测评类型（AI对话/题库）、风险等级等条件筛选要导出的数据。

- 导出预览：筛选条件确认后，显示导出数据的条数、包含字段的预览，供管理员确认后再执行导出操作。

- 导出格式：至少支持导出为Excel（.xlsx）格式，保证数据结构的完整性；可选支持导出为CSV格式。

- 导出记录：显示该管理员的历史导出记录，包含导出时间、筛选条件、文件大小、下载状态（可下载/已过期），支持重新下载（导出文件保留7天）。

#### 2.2.5.2 操作日志页

- 日志记录：系统自动记录关键操作，包括但不限于：登录、增删改题库、修改AI配置、数据导出/删除、权限变更、用户账号管理等。

- 日志信息：每条日志应包含：操作时间、操作人、操作模块、具体动作、IP地址、操作结果（成功/失败）、备注（如失败原因）。

- 日志筛选与搜索：支持按操作时间范围、操作人、操作模块、操作结果筛选日志；支持按关键词搜索日志内容。

- 日志导出：支持将筛选后的日志导出为Excel格式，用于审计或存档。

#### 2.2.5.3 问题反馈处理页

- 反馈列表：集中展示用户提交的反馈意见，列表信息包括：用户昵称/ID、反馈时间、反馈内容、反馈类型（功能问题/体验建议/测评结果疑问）、状态（待处理/处理中/已回复/已关闭）。

- 状态更新：管理员可以更新反馈的处理状态，并记录处理意见（如“已优化AI对话话术”、“该问题属于正常测评逻辑，已向用户解释”）。

- 回复用户：提供功能允许管理员直接在后端回复用户，回复内容可通过站内信或邮件通知用户；支持查看用户反馈的完整上下文（如用户使用的设备、APP版本、当前测评进度/结果）。

- 反馈统计：页面顶部显示反馈统计指标（如“本月总反馈数”、“待处理反馈数”、“各类型反馈占比”），帮助管理员掌握用户反馈情况。
> （注：文档部分内容可能由 AI 生成）