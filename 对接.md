
---

# 1. 登陆注册 (v2.0 - 含管理员功能)

### 一、 注册页面 (Register Page)

**前端任务更新**：

1. **普通用户注册**：界面保持不变（手机号、昵称、密码）。
2. **管理员注册**：需要在界面上增加一个**“注册为管理员”**的勾选框或切换按钮。
* 选中后，需额外显示一个输入框：**“管理员密钥”**。
* *(注：管理员密钥是在后端配置写死的字符串，例如 `SoulStation2025_Admin`，用于防止随意注册管理员)*。



#### 注册接口

* **接口地址**：`POST /api/auth/register`
* **Content-Type**: `application/json`

**场景 A：注册普通用户 (Request JSON)**

前端不传 `role`，或者传 `user`。

```json
{
  "phone": "13800138000",
  "password": "my_secret_password",
  "nickname": "SoulUser_01"
}

```

**场景 B：注册管理员 (Request JSON) —— 【新增】**

前端必须传 `role: "admin"` 和 `admin_key`。

```json
{
  "phone": "13900000000",
  "password": "admin_password",
  "nickname": "Admin_01",
  "role": "admin",                // 必填，指定角色
  "admin_key": "SoulStation2025_Admin"  // 必填，必须与服务器配置一致
}

```

* **后端返回给前端的数据 (Response)**：

```json
{
  "code": 200,
  "msg": "管理员注册成功，请登录",
  "data": null
}

```

*(异常情况处理)*：

* 如果 `role` 是 `admin` 但密钥错误：返回 `code: 403, msg: "管理员密钥错误"`。
* 如果手机号已存在：返回 `code: 400, msg: "该手机号已注册"`。

---

### 二、 登录页面 (Login Page)

**前端任务更新**：

1. 表单保持不变（手机号、密码）。
2. **【核心逻辑变更】**：点击登录后，前端拿到后端返回的 JSON。
3. **判断跳转**：
* 检查 `data.role` 字段。
* 如果 `role === 'admin'` -> 跳转到 **管理员后台首页** (`/admin/dashboard`)。
* 如果 `role === 'user'` -> 跳转到 **普通用户首页** (`/home`)。


4. **存储**：将 `uid`, `token`, `role` 都存入 `localStorage`。

####  登录接口

* **接口地址**：`POST /api/auth/login`
* **前端需要传的数据 (Request JSON)**：

```json
{
  "phone": "13800138000",
  "password": "my_secret_password"
}

```

* **后端返回给前端的数据 (Response) —— 【新增 role 字段】**：

```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "uid": 1001,
    "nickname": "SoulUser_01",
    "token": "eyJhbGciOiJIUzI1...",  
    "avatar_url": "https://example.com/default.png",
    "role": "user"   // 或者 "admin"。前端根据这个值决定跳转去哪里
  }
}

```

*(如果密码错误，返回 `code: 401, msg: "账号或密码错误"`)*



---
# 2. 首页 (Home Page) - 测评方式选择

**前端任务说明**：

* **调用时机**：仅在用户 **已登录** 时调用。
* **数据展示逻辑**：
* **顶部导航**：使用 `user_info` 渲染头像和昵称。
* **改善追踪**：根据 `tracking_reminder.show` 判断是否在核心入口附近展示“复测提醒 Banner”。
* **历史记录**：在页面下方展示 `history_records` 列表（如最近的一条或多条）。



#### 接口定义

* **接口地址**：`GET /api/home/index`
* **请求参数**：

| 参数名 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `uid` | Number | **是** | 用户ID |

* **响应数据 (Response)**：

```json
{
  "code": 200,
  "msg": "获取成功",
  "data": {
    // 1. 用户基础信息 (用于顶部导航)
    "user_info": {
      "nickname": "SoulUser_01",
      "avatar_url": "https://example.com/avatar.png"
    },

    // 2. 改善追踪提醒 (PRD: "展示改善追踪提醒")
    "tracking_reminder": {
      "show": true,                  // true=显示提醒, false=不显示
      "message": "距离上次测评已过30天，建议进行复测以追踪改善情况。"
    },

    // 3. 历史记录 (PRD: "展示历史记录")
    // 返回最近的测评记录摘要，用于首页展示
    "history_records": [
      {
        "id": 101,
        "date": "2025-12-01",
        "mode": "ai_chat",           // 测评方式
        "risk_level": "mild",        // 风险等级
        "summary": "轻度焦虑倾向"     // 结果标签
      }
    ]
  }
}

```

